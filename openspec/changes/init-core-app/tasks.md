# Tasks

## 1. Project Setup
- [ ] 1.1 Install dependencies (`react-router-dom`, `idb`, `lucide-react` for icons).
- [ ] 1.2 Setup basic React Router configuration in `src/App.tsx` and `src/main.tsx`.

## 2. Core Logic
- [ ] 2.1 Implement `src/utils/srs.ts` containing the SM-2 algorithm logic.
- [ ] 2.2 Implement `src/utils/db.ts` for IndexedDB schema and operations (decks, cards, logs).

## 3. UI Components
- [ ] 3.1 Create global CSS variables in `index.css` for the Chemistry Lab aesthetic.
- [ ] 3.2 Create `src/components/Flashcard.tsx` with 3D flip animation.
- [ ] 3.3 Create `src/pages/ReviewSession.tsx` to handle the queue and rating logic.

## 4. Verification
- [ ] 4.1 Test the review session with dummy data to ensure cards schedule correctly.
